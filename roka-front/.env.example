# URL del backend API
# En desarrollo: se usa el proxy de Vite, por lo que basta con /api
# En producción: colocar la URL completa del backend, ej: https://api.roka.cl/api
VITE_API_URL=/api

# Gemini API Key (opcional, para funcionalidades de IA)
VITE_GEMINI_API_KEY=